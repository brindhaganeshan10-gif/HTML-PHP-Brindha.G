47) Delete an element from the below array. And print the array elements in PHP. $x = array (1, 2, 3,
4, 5);
&lt;?php
$x = [1, 2, 3, 4, 5];
unset($x[2]);
$x = array_values($x);
echo implode(&#39; &#39;, $x), PHP_EOL;

OUTPUT:
1 2 4 5
